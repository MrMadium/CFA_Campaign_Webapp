/* IMPORT Libraries and consts, like Socket instances. */
const map_info = require("../config/config.json");

/* Create a map instance. */


/* Bring in the database planned route geom as one layer, one colour code (probably green for Christmas.).*/


/* Bring in the recorded instances (if any) of the route. Colour code as red. */


/* Set Marker parameters. See if we can bring in a custom Santa face, firetruck or something like that.*/


/* Do we add additional "float marker" information about the appliance? Appliance name? ID? */